pipeline {
    agent {
        label "jenkins-jx-base"
    }
    stages {
        stage('build') {
            steps {
                sh 'pwd'
                sh 'ls -lah'
            }
        }
    }
}
